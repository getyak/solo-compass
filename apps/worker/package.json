{
  "name": "@solo-compass/worker",
  "version": "0.0.1",
  "private": true,
  "description": "Durable evidence refresh worker for Solo Compass",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@solo-compass/data": "workspace:*",
    "@solo-compass/sources-core": "workspace:*",
    "@solo-compass/sources-osm": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.2",
    "vitest": "^3.2.7"
  }
}
